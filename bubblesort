def bubble_sort(List):
	for i in range(0,len(List)-1):
		for j in range(0,len(List)-1-i):
			if List[j]>List[j+1]:
				List[j],List[j+1] = List[j+1],List[j]
	return List
TheList =[4,2,1,32,24,65,47,0,5]
print(bubble_sort(TheList)) 

